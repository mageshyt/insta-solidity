import React from "react";
import { useAppContext } from "../../context/context";

const Posts = () => {
  const { userAddress, posts }: any = useAppContext();

  return <div>
   
  </div>;
};

export default Posts;
